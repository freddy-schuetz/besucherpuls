import Ansicht from "@/components/Ansicht";

/** Startseite: alle Messpunkte über vier Länder — der Beleg, dass die Zahlen echt sind.
 *  Die drei Schaufenster darunter zeigen, was sich damit anfangen lässt. */
export default function Seite() {
  return <Ansicht />;
}
